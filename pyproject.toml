[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gazefix"
version = "0.1.0"
description = "Local Windows webcam and face-tracking foundation for GazeFix"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "mediapipe==0.10.35",
    "numpy>=1.26,<3",
    "opencv-contrib-python>=4.10,<5",
    "PySide6>=6.7,<7",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
]

[project.scripts]
gazefix = "gazefix.main:main"
gazefix-camera-test = "gazefix.camera.diagnostics:main"
gazefix-tracking-validate = "gazefix.tracking.offline_validation:main"

[tool.setuptools.packages.find]
include = ["gazefix*"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
