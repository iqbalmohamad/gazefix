.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.idea/
.vscode/

experiments/

# Model-feasibility spike: upstream clone and weights are local only
spike/liveportrait/upstream/

# Maxine Phase 1A research spike. Footage, derived inputs, model outputs and
# the blind package live under the already-ignored experiments/ tree; only
# manifests and hashes are committed. NVIDIA's client is fetched locally.
research/maxine-eye-contact/**/nvidia-client/

# Maxine Phase 1B runtime experiment. Sources, live-produced input, corrected
# output and timing series live under the already-ignored experiments/ tree;
# only the harness, its tests and small manifests are committed. NVIDIA's
# client is fetched locally.
research/maxine-eye-contact-phase1b/**/nvidia-client/
